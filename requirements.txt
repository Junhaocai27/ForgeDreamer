# === 1. 指定 PyTorch 官方 CUDA 12.1 镜像源 ===
--extra-index-url https://download.pytorch.org/whl/cu121

# === 2. 核心深度学习框架 (已更新为 2.3.1+cu121) ===
torch==2.3.1+cu121
torchvision==0.18.1+cu121

# === 3. 视觉与扩散模型核心库 ===
diffusers==0.33.1
transformers==4.49.0
huggingface_hub==0.31.4
accelerate==1.7.0
safetensors==0.5.3
xformers==0.0.27

# === 4. 图像、视频与科学计算 ===
Pillow==11.2.1
opencv-python==4.11.0.86
imageio==2.31.1
imageio-ffmpeg==0.6.0
scipy==1.13.1
numpy>=1.23.0  # 强制使用支持 Python 3.10 的免编译版本
pandas>=1.5.0  # 强制使用支持 Python 3.10 的免编译版本

# === 5. 实用工具与杂项 ===
pip==26.0.1
tqdm==4.67.1
mediapipe==0.10.21
fire==0.5.0
PyYAML==6.0.2
wandb==0.20.1
triton==2.3.1
gradio  # 注意：在你提供的参考列表中未找到 gradio 的版本号，因此保留原样
plyfile==0.8.1
ftfy==6.3.1
tensorboard==2.19.0
# dhg==0.9.5 # 需要搭配当前的 torch 2.3.1+cu121
einops==0.8.1